ActivityDocument
Wizard
Status Tile
CBatchHeaderTileDlg
CParsedDialog
SlaveFormView e CParsedDialogWithTiles - logica  gestionale (es. SlaveFormView di Bilanci&Oltre negli Schemi di riclassificazione)
BSP da portare in TaskBuilder
Tag booleani valorizzabili con una variabile (come il tag 'activation')
AddTab per CTilePanel (esempio castelletto IVA in documenti emessi/ricevuti, ammortamento per divisa)
AddDropdownMenuItem in Client Doc
Add di un bottone alla toolbar da ClientDoc (tipo inspector)
strActivation in bottoni della toolbar, in certi casi non funziona(Elisa C.)
Customizzazione toolbar: nascondere toolbar 'tools'. (esigenza raggirata con un json addittivo di una nuova toolbar da zero, invece che con json sottrattivo )
Customizzazione toolbar: l'aggiunta di separatori non funziona sempre se aggiunti come primo pulsante (il problema era già presente prima del json) (es Liquidazione IVA/Tax Settlement/BOOL CTaxSummaryJournalsPrintFrame::OnCustomizeJsonToolBar())

DONE Customizzazione toolbar: Metodo aggiuntivo per poter chiamare AddDropDownItem dopo la creazione della toolbar (ora gestito in OnPopulatedDropDown)
Customizzazione toolbar: non gestita customizazzione colore della toolbar, es: CAccountingSimulationsSelectionFrame::OnCustomizeTabbedToolBar
CTBBaseNavigationFrame::OnPopulatedDropDown(UINT nIDCommand) - Come e se vengono migrati i bottoni della toolbar

TBActivityDocument - Regressione rispetto ad ActivityDocument:
in CustomizebodyEdit non si riesce a chiamare metodi specifici per CTreeBodyEdit (ad esempio: ShowExpandCollapseAllBtn(); ShowExpandCollapseNodeBtn();ShowAlwaysTreeStruct();)

Lo splitter? (adesso viene utilizzato in tutte le navigazioni grafiche)
//-----------------------------------------------------------------------------------------------
BOOL CTBBaseNavigationFrame::CreateAuxObjects(CCreateContext*	pCreateContext)
{
	CTBBaseNavigationDocument* pDoc = (CTBBaseNavigationDocument*)GetDocument();

	switch (pDoc->m_eSplitType)
	{
		case E_BASE_NAVIGATION_SPLITTYPE::BASE_NAVIGATION_HORIZONTAL:
			pDoc->m_pSplitter = CreateSplitter(RUNTIME_CLASS(CTaskBuilderSplitterWnd), 2, 1);	//TODO - generalization m,n ?!
			break;
		case E_BASE_NAVIGATION_SPLITTYPE::BASE_NAVIGATION_VERTICAL:
			pDoc->m_pSplitter = CreateSplitter(RUNTIME_CLASS(CTaskBuilderSplitterWnd), 1, 2);	//TODO - generalization m,n  ?!
			break;
	}
	. . .

Come risolveremo vecchio OnUpdateTitle (aggiornamento del titolo della tile). Adesso nel tbjson sulla tile => "text": "{{MiaVariabile}}" => non funziona  

CParsedDialog;
Possibilità di aggiungere item, ad esempio da ClientDoc, in una posizione specifica (bottoni toolbar, tile, filtri etc)


Silvano: dovremmo fare un ulteriore meccanismo per {{ }} per cui nel motore json non si valuta solo il dataobj e si assegna, ma se ne assegna il reference? (es. SetTileClickable,        SetLeftBorderColor(&pDoc->m_ColorStatusNF);//COLORREF)
