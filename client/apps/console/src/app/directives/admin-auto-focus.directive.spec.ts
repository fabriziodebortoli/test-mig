import { AdminAutoFocusDirective } from './admin-auto-focus.directive';

describe('AdminAutoFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminAutoFocusDirective();
    expect(directive).toBeTruthy();
  });
});
