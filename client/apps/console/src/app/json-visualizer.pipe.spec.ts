import { JsonVisualizerPipe } from './json-visualizer.pipe';

describe('JsonVisualizerPipe', () => {
  it('create an instance', () => {
    const pipe = new JsonVisualizerPipe();
    expect(pipe).toBeTruthy();
  });
});
